# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://ssahubjlrmymvwszpyra.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_PROJECT_ID=ssahubjlrmymvwszpyra

# --- OpenAI (add later — app runs in mock mode without it) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- OAuth (configured inside Supabase Auth providers dashboard, not here) ---
# Google and GitHub client IDs/secrets are set in the Supabase dashboard under
# Authentication > Providers. No app-level env vars are needed for them.

# --- App ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Stripe (billing — wire up when ready) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PRICE_PRO=
